interface:
  display_name: "pre-vibe"
  short_description: "Turn vague starts into a clean Codex intake workflow"
  default_prompt: "Before starting work, turn my rough request into a clear project-specific spec, AGENTS guidance, and first prompt."
  brand_color: "#2563EB"
policy:
  allow_implicit_invocation: true
